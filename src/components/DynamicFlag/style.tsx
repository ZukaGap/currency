import {StyleSheet} from 'react-native';

const getStyleObj = () => {
  return StyleSheet.create({
    flagContainer: {flex: 1, borderRadius: 4},
    empty: {width: 40, height: 50},
  });
};

export default getStyleObj;
